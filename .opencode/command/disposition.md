---
description: 去向前复评并给出去向建议
agent: acuflow
---

请使用 `disposition-knowledge` 技能并调用 `episode_status`。

只针对 `missing` 追问：最新状态、未解决问题、待回报结果、随访条件。

给出明确的去向建议（住院/转院/出院）、依据与限制，列出仍未排除的危险问题与交接/复诊提醒，产出结论草案并等待我确认。
